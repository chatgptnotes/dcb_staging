@extends('public.layout')
@section('show_public_nav','yes')
@section('title','Create your account — DecodeMyBrain')
@push('styles')<style>.auth-page{max-width:620px;margin:0 auto}.auth-page .brand{margin-bottom:35px}.auth-page h1{margin:0}.auth-page .lead{margin:15px 0 32px}.auth-page .button{width:100%;margin-top:26px}.auth-page .login-link{text-align:center;color:var(--muted)}</style>@endpush
@section('body')<main class="page"><div class="auth-page"><a class="brand" href="{{ route('landing') }}"><span class="brand-mark">⌁</span>DecodeMyBrain</a><h1 class="heading">Create your account</h1><p class="lead">Takes a minute. Your progress is saved as you go.</p>
@if(session('fail'))<div class="alert">{{ session('fail') }}</div>@endif
@if($errors->any())<div class="alert">Please check the highlighted details.</div>@endif
<form method="post" action="{{ url('sign-up') }}">@csrf<input type="hidden" name="intended_package" value="{{ session('intended_package') }}"><input type="hidden" name="purchase_flow" value="{{ session('new_purchase_flow') ? 1 : '' }}"><div class="form-grid"><div class="field"><label>First name</label><input name="first_name" value="{{ old('first_name') }}" required></div><div class="field"><label>Last name</label><input name="last_name" value="{{ old('last_name') }}" required></div><div class="field"><label>Username</label><input name="user_name" value="{{ old('user_name') }}" required></div><div class="field"><label>Date of birth</label><input type="date" name="dob" value="{{ old('dob') }}" required></div><div class="field full"><label>Email</label><input type="email" name="email" value="{{ old('email') }}" required></div><div class="field full"><label>Phone</label><input name="phone" value="{{ old('phone') }}" placeholder="+44 7700 000000"></div><div class="field"><label>Password</label><input type="password" name="password" minlength="6" required></div><div class="field"><label>Confirm password</label><input type="password" name="password_confirmation" required></div></div><button class="button" type="submit">Create account</button><p class="login-link">Already registered? <a class="text-link" href="{{ url('sign-in') }}">Log in</a></p></form></div></main>@include('public.partials.footer')@endsection
